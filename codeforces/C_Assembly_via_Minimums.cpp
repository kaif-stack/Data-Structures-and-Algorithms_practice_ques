#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n, size;
        cin>>n;
        size = (n*(n-1))/2;
        int arr[size];
        for(int i=0; i<size; i++)
        {
            cin>>arr[i];
        }
        sort(arr, arr+n);
        int  =

    }
}